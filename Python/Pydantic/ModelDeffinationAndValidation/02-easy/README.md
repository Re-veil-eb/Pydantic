# Easy Challenges

## Scenario
Models with regex validation, aliases, and stricter constraints:
- Customer model with regex-based email validation.
- Age range enforcement.
- Alias support for user_id (uid).

## Why It Matters
Shows ability to:
- Enforce regex patterns.
- Handle field aliases.
- Apply business rules with constraints.

## Tests
Run `pytest` to validate:
- Regex email enforcement.
- Age range enforcement.
- Alias mapping works correctly.
